# 다음_세션_프롬프트

## 목적

다음 세션에서 SafeLink 작업을 이어갈 때, 이미 합의된 문서와 실제 업로드 상태를 먼저 확인하고 그 기준에 맞춰서만 수정하기 위한 내부 지침이다.

## 가장 중요한 반성 포인트

- 사용자가 요구한 것은 `먼저 확인`인데, 이전 세션에서는 `조금 확인하고 추론`하는 방식으로 답해서 계속 꼬였다.
- 로컬 git 확인과 GitHub 웹 확인을 구분하지 않고 말해서 신뢰를 잃었다.
- 이미 끝난 일과 앞으로 할 일을 분리하지 않고 섞어서 말해서 혼선이 생겼다.
- 사용자가 시키지 않은 확장 설명과 방향 전환을 해서 문서 기준이 흐려졌다.

## 다음 세션 작업 원칙

```text
추정하지 말고 먼저 확인한다.
사용자가 질문하면 먼저 폴더 안 파일, 합의 문서, 필요 시 노션과 GitHub를 확인한 뒤 답한다.
로컬에서 확인한 것인지, GitHub 웹에서 확인한 것인지, 노션에서 확인한 것인지 반드시 구분해서 말한다.
이미 한 것, 지금 확정된 것, 앞으로 할 것을 섞지 말고 분리해서 정리한다.
사용자가 시키지 않은 방향 확장, 기능 추가, 문장 재해석을 하지 않는다.
합의된 문서 방향을 임의로 바꾸지 않는다.
문서 수정이 필요하면 설명보다 먼저 실제 파일을 수정한다.
사용자가 지정한 파일만 수정하고, 지정한 파일만 GitHub에 올린다.
```

## 우선 확인할 기준 문서

다음 문서들을 현재 SafeLink 작업의 기준 문서로 본다.

- `프로젝트_설명문서_초안.md`
- `교수님_질문_답변_정리.md`
- `앱_기능별_역할분담.md`
- `safelink_로드맵.md`
- 노션 최신 합의 내용

## 문서 해석 규칙

- `프로젝트_설명문서_초안.md`의 방향이 현재 프로젝트 설명 기준이다.
- `교수님_질문_답변_정리.md`는 반드시 아래 3가지 질문만 기준으로 유지한다.
  - 우리 앱이 LLM AI와 차별점이 무엇인가
  - 데이터는 어디서 오는가
  - AI를 어떻게 학습시킬 것인가
- `앱_기능별_역할분담.md`는 장기 역할이 아니라, 사용자가 합의한 주차별/현재 단계 기준 역할로 맞춘다.
- `safelink_로드맵.md`는 팀원 직책을 임의로 붙이지 말고, 사용자가 합의한 내용 기준으로만 적는다.

## 답변 순서

질문을 받으면 아래 순서로 처리한다.

1. 폴더 안 관련 파일부터 확인한다.
2. 합의 문서와 현재 파일 내용의 차이를 찾는다.
3. 필요한 부분만 수정한다.
4. 수정한 파일명과 수정 결과만 짧고 명확하게 보고한다.

## GitHub 관련 규칙

- 사용자가 `깃허브 들어가서 확인해`라고 하면 로컬 git 로그가 아니라 GitHub 웹 페이지를 실제로 열어 확인한다.
- GitHub 웹 확인 결과와 로컬 git 확인 결과를 절대 섞어서 표현하지 않는다.
- 사용자가 올리라고 한 파일만 `git add`, `commit`, `push` 한다.
- 다른 파일 변경사항을 멋대로 같이 올리지 않는다.

## 사용자 응답 스타일

```text
변명하지 말고 핵심만 답한다.
먼저 결과를 말하고, 필요한 설명만 짧게 덧붙인다.
질문이 파일 수정 요청이면 바로 수정부터 한다.
사용자가 요청하지 않은 제안은 덧붙이지 않는다.
```

## 이번 세션에서 실제로 확정된 핵심

- 팀원들이 올린 내용은 GitHub 웹 저장소 `sunhan2002-cyber/safelink` 기준으로 직접 확인했다.
- 김선한 관련 문서, 김우영 관련 `.wy` 파일, 신기훈 관련 위험 분석/키워드/시나리오 커밋, 김재겸 관련 로드맵 커밋이 확인됐다.
- 사용자는 `확인한 사실만 말하라`, `폴더 파일부터 먼저 확인하라`, `이미 한 것과 앞으로 할 것을 구분하라`는 기준을 매우 중요하게 본다.

## 다음 세션 시작용 한 줄 프롬프트

```text
SafeLink 작업을 이어간다. 먼저 폴더 안 관련 파일과 합의 문서, 필요 시 노션과 GitHub 웹을 확인하고, 이미 한 것/지금 확정된 것/앞으로 할 것을 분리해서 답해라. 추정하거나 방향을 넓히지 말고, 사용자가 지정한 파일만 정확히 수정해라.
```
