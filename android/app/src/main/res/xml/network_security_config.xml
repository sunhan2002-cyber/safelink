<?xml version="1.0" encoding="utf-8"?>
<!-- 평문(HTTP) 통신은 개발용 로컬 분석 서버에만 허용한다.
     예전에는 매니페스트의 usesCleartextTraffic="true" 로 앱 전체에서 평문 통신이 열려 있어,
     중간에서 내용을 보거나 바꿔치기할 수 있는 경로가 남아 있었다.
     10.0.2.2 는 에뮬레이터에서 개발 PC 를 가리키는 주소이고, 실기기에서는 닿지 않는다.
     실제 배포 서버는 HTTPS 를 쓰므로 이 예외가 필요 없다. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
