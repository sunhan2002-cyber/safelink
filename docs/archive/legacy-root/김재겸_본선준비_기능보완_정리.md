# 본선 준비 — 기능 보완 작업 정리

- 작성: 김재겸 (기능확장)
- 일자: 2026-09-09
- 대상 커밋: `dfad2ee`, `d8b4512`, `c4945fb`
- 이전 정리 문서: `김재겸_자가진단_검사기록_구현정리.md` (자가진단 채점 + 검사 기록 Room 도입)

---

## 1. 이번 작업의 배경

본선 진출 후, **보고서에는 기능으로 적혀 있으나 실제로는 동작하지 않던 항목**을 우선 정리했습니다.
시연 중 심사위원이 직접 눌러볼 수 있는 화면(설정 탭)에 미동작 항목이 몰려 있어 위험도가 높았습니다.

---

## 2. 백그라운드 감지 — 문자(SMS) 확대

### 무엇을 했나

감지 대상에 문자 앱을 추가했습니다.

```kotlin
private val MONITORED_PACKAGES = setOf(
    "com.kakao.talk",                    // 카카오톡
    "com.samsung.android.messaging",     // 삼성 메시지(갤럭시 기본 문자)
    "com.google.android.apps.messaging", // Google 메시지
)
```

### 왜 이렇게 간단한가

접근성 서비스 설정(`accessibility_service_config.xml`)에 `packageNames` 제한을 두지 않아 **서비스가 이미 모든 앱의 이벤트를 받고 있었습니다.** 실제 분석 대상은 위 목록으로만 걸러내는 구조라, 패키지명 추가만으로 확장됩니다.

> 보고서에 쓴 "패키지 등록만으로 확장 가능" 주장이 실제로 증명된 셈입니다.

### 검증

에뮬레이터에 스미싱 문자를 발송해 전체 흐름을 확인했습니다.

1. 문자 수신 → 경고 알림 즉시 표시 (`감지된 표현: "신청하신 적 없는 카드"`)
2. 기록 탭에 자동 저장 (`백그라운드 감지 · 보이스피싱` / 경고)

---

## 3. 중복 알림 억제

### 발견한 문제

문자 목록 화면과 대화방 화면에서 같은 문자가 **각각 감지되어 기록이 2건씩 쌓였습니다.** 목록엔 잘린 미리보기, 대화방엔 전문이 보이므로 기존의 "직전과 같은 텍스트면 건너뛰기" 로직으로는 걸러지지 않았습니다.

### 해결 방식

같은 앱에서 **매칭된 키워드 집합이 동일**하면 같은 사건으로 보고 억제합니다.

| 상황 | 창(window) id | 동작 |
|---|---|---|
| 같은 대화방에서 스크롤 | 동일 | 30분간 억제 |
| 대화방을 나갔다 다시 들어옴 | 변경 | **다시 알림** |
| 목록 → 대화방 즉시 이동 | 변경 | 60초 내 이동은 한 번의 확인 행동으로 보고 억제 |

다른 앱에서 온 같은 문구는 별개 사건이므로(문자·카톡 각각 뿌려지는 경우가 실제로 많음) 서명에 패키지명을 포함해 따로 알립니다.

### 검증

- 목록 → 대화방 → 뒤로 → 재진입(화면 전환 4회) → **기록 1건**
- 60초 경과 후 재진입 → **2번째 기록 생성**

---

## 4. 설정 탭 미완성 기능 (보고서에 적혀 있었으나 미동작이던 항목)

### 4-1. 생체인증

- `androidx.biometric` 도입, `BiometricAuth` 헬퍼 신설
- 잠금 화면 진입 시 자동 시도 + 지문 버튼으로 수동 시도, 실패·취소 시 PIN 입력으로 폴백
- 설정 토글을 실제 저장소와 연결. 앱 잠금이 꺼져 있거나 등록된 생체정보가 없으면 켜지지 않고 사유를 안내

> `BiometricPrompt`가 `FragmentActivity`를 요구해 `MainActivity`의 상속을 `ComponentActivity` → `FragmentActivity`로 변경했습니다. `FragmentActivity`가 `ComponentActivity`의 하위 클래스라 기존 Compose·ViewModel 코드는 그대로 동작합니다.

### 4-2. PIN 5회 실패 차단 (Design.md 5.4)

- 연속 5회 실패 시 30초 입력 차단, 남은 시간 1초 단위 표시
- 실패할 때마다 남은 시도 횟수 안내, 성공 시 실패 이력 초기화
- 차단 중에는 키패드·생체인증 버튼 모두 비활성화

**검증**: 4회 실패 → "1번 더 틀리면 잠시 입력이 제한됩니다" / 5회 → "27초 후 다시 시도해 주세요"(카운트다운) / 차단 중 키패드 무반응 / 30초 후 정상 진입

### 4-3. 알림 문구 설정 (Design.md 7장)

가해자와 화면을 함께 볼 수 있는 상황을 대비해, **위험 내용을 드러내지 않는 알림 문구**로 바꿀 수 있게 했습니다.

- 기본값: 제목 "알림" / 내용 "새로운 알림이 있습니다" (Design.md 7장 규정값)
- 켜면 위험도·감지 표현이 알림에 전혀 노출되지 않음
- 설정에서 문구를 직접 편집 가능

**검증**: 중립 문구 ON 후 실제 스미싱 문자 발송 → 알림이 "알림 / 새로운 알림이 있습니다"로만 표시됨

> 참고: 문구는 감추되 아이콘은 경고 삼각형을 유지하고 위험도별 강조색(빨강·주황)만 제거했습니다. 아이콘까지 완전히 중립화하려면 앱 아이콘 자체를 정해야 하는데, Design.md에 "추후 팀 결정"으로 남아 있어 건드리지 않았습니다.

### 4-4. 홈 카운터

"오늘의 알림"·"정밀 검사"를 기록 DB에서 **오늘 0시 기준**으로 집계하도록 변경했습니다.

- 기존: 메모리 카운터 → 앱 재시작 시 0으로 초기화됨. "정밀 검사"는 아예 `0` 하드코딩
- 변경: DB 집계 → 재시작해도 유지되고, 날짜가 바뀌면 자동으로 0부터

**"정밀 검사"의 정의**: 사용자가 직접 실행한 분석(텍스트·스크린샷) 건수. 백그라운드 자동 감지인 "오늘의 알림"과 대비되는 개념으로 정했습니다.

---

## 5. 분석 결과 원문 하이라이트

결과 화면에 **"분석한 내용"** 카드를 추가해, 원문에서 매칭된 구간에 배경색·밑줄을 입혔습니다.

```
"택배기사인데요, 배송 중 확인 차 연락드렸습니다.
 그럼 명의 도용 우려가 있어서 확인이 필요합니다.
 지금 당장 확인 안 하시면 계좌가 압류될 수 있습니다."
  └ 강조 구간: 택배기사 / 배송 중 / 명의 도용 우려 / 지금 당장 / 압류
```

기존에는 매칭된 표현을 별도 목록으로만 나열해 **어떤 문맥에서 걸렸는지** 보이지 않았습니다. `MatchedKeyword`의 `startIndex`/`endIndex`를 사용하며, 구간이 겹치거나 범위를 벗어난 값은 건너뛰어 엔진이 바뀌어도 화면이 깨지지 않게 했습니다.

---

## 6. 홈 최근 검사 기록

기록 DB에서 최신 3건을 위험도 배지·요약과 함께 홈에 노출합니다. 기록이 없으면 섹션 자체를 숨겨 첫 실행 화면이 비어 보이지 않게 했습니다.

---

## 7. 작업 중 발견해 고친 문제

| 문제 | 조치 |
|---|---|
| 설정 캡션에 코드가 그대로 출력 (`현재: "${'$'}{NotificationTextStore.title(context)}"`) | 문자열 템플릿 이스케이프 오류. 수정 후 `현재: "알림"`으로 정상 표시 |
| 메모 화면 "메모는 **암호화되어** 저장됩니다" | 실제로는 평문 저장이므로 "서버로 전송되지 않고 이 기기에만 저장됩니다"로 정정 |
| 결과 화면에 "원문 하이라이트 구현 필요" TODO 주석 잔존 | 구현 완료에 맞춰 주석 갱신 |

---

## 8. 검증 요약

- **단위 테스트 113개 전부 통과** (앱 잠금 정책 3건 신규 추가)
- 에뮬레이터 실기 조작으로 전 항목 확인 (스미싱 문자 수신 → 알림 → 기록 저장 → 상세 복원 → 메모 → 필터 → 전체 삭제까지)

---

## 9. 팀 확인이 필요한 사항

### 9-1. AI 연동을 백그라운드까지 확장할지 (보류 중)

수동 분석에는 AI 보조분석이 붙어 있으나, **백그라운드 감지는 의도적으로 AI를 호출하지 않습니다.**

이유는 전송되는 내용의 성격이 다르기 때문입니다.

| | 수동 분석 | 백그라운드 |
|---|---|---|
| 전송 대상 | 사용자가 직접 고른 대화 | 화면에 떠 있는 모든 텍스트(최대 5,000자) |
| 사용자 인지 | 붙여넣기·업로드하며 인지 | 인지하지 못함 |
| 범위 | 선택한 대화만 | 상대방 메시지, 옆 대화방 미리보기, 알림 내용까지 |

마스킹은 전화번호·URL만 가리고 **대화 본문은 그대로 전송**되므로, 백그라운드까지 확장하면 사용자가 인지하지 못한 제3자 대화가 서버로 나갈 수 있습니다. 보고서·요구사항 문서에도 백그라운드는 서버 전송이 없다고 명시해 제출한 상태입니다.

**선택지**: (A) 매칭 구간만 전송 / (B) 고위험 건만 / (C) 별도 동의 토글(기본 OFF) / (D) 현재 유지
→ 팀 논의 후 결정 필요

### 9-2. "수동 신고" 조건이 죽어 있음

보고서 4번에 "사용자가 직접 '위험한 것 같다'고 수동으로 신고한 경우 AI 호출"이라고 적었으나, 해당 파라미터(`manualReportFlag`)를 넘기는 화면이 없어 항상 `false`입니다. 결과 화면에 버튼 하나를 추가하면 살아납니다.

### 9-3. AI 호출 조건의 "신뢰도"는 자동 측정이 아님

신규 세부 유형 8종(로맨스스캠 `2-6`~`2-10`, 가스라이팅 `3-7`~`3-9`)이 매칭되면 AI를 호출하는데, 이 목록은 **정확도를 측정해 정한 것이 아니라 "4주차에 새로 만든 규칙"이라 사람이 지정한 하드코딩 목록**입니다. 심사에서 "검증이 충분한지 어떻게 판단하나요?"라는 질문이 나오면 이 점을 정확히 답해야 합니다.

---

## 10. 남은 작업

**단독으로 진행 불가 (외부 입력 필요)**
- AI API 실연동 — API 키 발급, 모델 선택, 배포 위치 결정 필요
- 개인정보 처리방침 링크 — 실제 방침 문구·URL 필요
- 인스타그램 DM 감지 — 실기기 + 인스타 계정으로 검증 필요

**본선 준비**
- 실기기 시연 리허설 (에뮬레이터로는 카톡 감지·실제 문자 수신을 완전히 재현할 수 없음)
- 보고서·발표자료 갱신 — 현재 보고서에 "메신저 확대 **예정**"으로 적혀 있으나 문자는 이미 완료됨. 자가진단·검사기록·홈 카운터·원문 하이라이트·생체인증도 미반영 상태
